<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="org.grupovision.sipmovil.fragments.DetailCaseFragment">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight=".3"
            android:id="@+id/layoutBack"
            android:background="@color/colorPrimary"/>

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight=".7"
            android:paddingTop="@dimen/img_size">

            <LinearLayout
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/app_name"
                    android:textSize="@dimen/font_title"
                    android:id="@+id/txtIdentificationNumber"
                    android:gravity="center"
                    android:textStyle="bold"
                    />
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/app_name"
                    android:textSize="@dimen/font_title"
                    android:id="@+id/txtName"
                    android:gravity="center"
                 />
           </LinearLayout>
        </ScrollView>
    </LinearLayout>

    <org.grupovision.sipmovil.utilities.RoundedImageView
        android:layout_width="@dimen/img_size"
        android:layout_height="@dimen/img_size"
        android:src="@drawable/img_profile"
        android:scaleType="centerCrop"
        app:layout_anchor="@id/layoutBack"
        app:layout_anchorGravity="bottom|center"
        android:id="@+id/imgProfile"
        />

</android.support.design.widget.CoordinatorLayout>
